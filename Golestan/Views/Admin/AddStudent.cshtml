@{
    ViewBag.Title = "افزودن دانشجوی جدید";
}
@*
<h2>افزودن دانشجوی جدید</h2>

<form method="post" action="/Admin/AddStudent">
    <label>نام کامل:</label>
    <input type="text" name="fullName" required />

    <label>نام کاربری:</label>
    <input type="text" name="username" required />

    <label>رمز عبور:</label>
    <input type="password" name="password" required />

    <button type="submit">ثبت</button>
</form> *@
@model List<Golestan.Models.Users>

<table>
    <thead>
        <tr>
            <th>نام</th>
            <th>ایمیل</th>
            <th>عملیات</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null)
        {
            @foreach (var user in Model)
            {
                @if (user.Id == Model[0].Id)
                {
  
                }
                else
                {
                    <tr>
                        <td>@user.First_name</td>
                        <td>@user.Email</td>
                        <td>
                            <form asp-action="AddStudent" asp-controller="Admin" method="post" style="display:inline;">
                                <input type="hidden" name="Id" value="@user.Id" />

                                <label>شماره دانشجویی:</label>
                                <input type="number" name="StudId" required />

                                @* <label>شماره دانشکده:</label>
                                <input type="number" name="DepartId" required /> *@

                                @* <label for="DepatId">دانشکده</label>
                                <select asp-for="مم" asp-items="ViewBag.Departments" class="form-control">
                                    <option value="">-- Select Department --</option>
                                </select> *@

                                <select name="DepartID" id="DepartID" class="form-control">
                                    <option value="">-- Select Department --</option>
                                    <option value="111">کامپیوتر</option>
                                    <option value="222">برق</option>
                                    <option value="333">مکانیک</option>
                                    <option value="444">معماری</option>
                                </select>


                                <button type="submit" onclick="return confirm('آیا مطمئن هستید؟')" style="background:none;border:none;color:blue;text-decoration:underline;cursor:pointer;">
                                    اضافه
                                </button>
                            </form>
                        </td>
                    </tr>
                }

            }
        }
    </tbody>
</table>

